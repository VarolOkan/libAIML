

# set the include path found by configure
INCLUDES = $(all_includes)

# the library search path.
lib_LIBRARIES = libstd_utils.a
libstd_utils_a_SOURCES = std_utils.cpp cconfig.cpp




AM_CXXFLAGS = -Werror -pedantic -Wall -ansi
pkginclude_HEADERS = cconfig.h std_util.h
METASOURCES = AUTO
